# Chat review: 587ae068-6879-4b20-ac80-5fa479e329d0

Source: SysForge agent transcript  
UUID: `587ae068-6879-4b20-ac80-5fa479e329d0`  
Path: `C:\Users\User\.cursor\projects\f-Codeing-Project-SysForge\agent-transcripts\587ae068-6879-4b20-ac80-5fa479e329d0\`  
Sibling subagent `*.jsonl`: none

## Title/topic

Fix Cursor rule frontmatter for `.cursor/rules/teaching-mode.mdc` so the rule can apply via intelligent matching (add `description`; keep `alwaysApply: false`).

## Features

- Updated `SysForge/.cursor/rules/teaching-mode.mdc` frontmatter:
  - Added `description` (YAML `>-` multiline) covering teaching/learning mode for SysForge (C#, .NET, Avalonia MVVM): mentor-style guidance, hints, pitfalls, architecture sketches, plans/pseudocode before code; triggers on teach/explain/avoid full solutions/“teaching mode.”
  - Left `alwaysApply: false` (Apply intelligently via description match, or `@teaching-mode`).
- No product/app features, migrations, UI, or API work in this chat.

## Issues/bugs

- Pre-fix: with `alwaysApply: false` and no `description` / no `globs`, Cursor only included the rule on explicit `@`-mention ([Cursor rules docs](https://cursor.com/docs/context/rules)).
- Post-fix (in SysForge): rule uses `description` + `alwaysApply: false`; body still defines teaching-mode behavior (minimal snippets, plans before code, C#/.NET practices).

## UX

- Agent UX only (not end-user SysForge UI): teaching mode stays opt-in by conversation intent, not always-on, so it does not fight normal “run commands / implement” project guidance.
- Alternatives discussed (not chosen): `alwaysApply: true`; file `globs` (e.g. `SysForge/**/*.cs`, `SysForge/**/*.axaml`); remove `description` and rely on `@teaching-mode` only.

## Plans/deferred

- No product roadmap or deferred feature work.
- Optional activation styles left as user choice (always-on vs globs vs manual-only); chat shipped description-based intelligent apply.

## Odyssey checklist

- [ ] Decide whether odysseus-sysforge needs an equivalent `.cursor/rules/teaching-mode.mdc` (repo currently has no `.cursor/rules/`).
- [ ] If porting: adapt `description` language from Avalonia/C#/.NET to Python/web (Flask/static) stack used by odysseus-sysforge; keep `alwaysApply: false` unless you want teaching mode on every chat.
- [ ] If porting: keep behavioral body (teach first, plans/pseudocode, minimal snippets) or rewrite for Odyssey conventions; do not treat this as a product migration item.
- [ ] Confirm no product behavior to port from this UUID (none present).
- [ ] Cross-check sibling SysForge rules already agent-requestable (`teaching-mode.mdc`, `simple-code-writeing.mdc`) if Odyssey agent workflows should match SysForge mentoring style.

## Confidence

**High** — short, complete transcript (5 turns); no subagent siblings; change is rule frontmatter only; product migration impact is none unless agent-rule parity is desired.
