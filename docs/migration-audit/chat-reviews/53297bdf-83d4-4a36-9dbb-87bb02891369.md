# Title/topic

Fix Cursor rule frontmatter for `SysForge/.cursor/rules/simple-code-writeing.mdc` (missing `description` / apply mode).

Source: [53297bdf](53297bdf-83d4-4a36-9dbb-87bb02891369) — SysForge agent chat. No sibling subagent `*.jsonl` in the transcript folder.

# Features

- Added YAML `description` to `simple-code-writeing.mdc` so Cursor can **Apply intelligently**.
- Kept `alwaysApply: false` (not always-on; not glob-bound).
- Rule body (unchanged in intent): section-by-section implementation — ≤10 lines or one cohesive unit, one primary file per step (paired test exception), no broad scaffolding/TODO placeholders, unified diff after each section, stop for review.

# Issues/bugs

- With `alwaysApply: false` and no `description` or `globs`, Cursor only applied the rule on `@`-mention; it looked unused.
- Docs reference: https://cursor.com/docs/context/rules

# UX

- N/A for product UI. Agent-workflow UX only: micro-sections + review stops when the rule is pulled in.

# Plans/deferred

- Optional later: `alwaysApply: true` if every chat should use section-by-section delivery.
- Optional later: `globs` (e.g. `SysForge/**/*.cs`, `SysForge/**/*.axaml`) if path-context attachment is preferred; agent noted globs fire on explain/small-edit chats too.
- Note: when `alwaysApply: true`, description and globs are ignored per Cursor docs.

# Odyssey checklist

- [ ] Decide whether odysseus-sysforge needs the same agent workflow rule.
- [ ] If yes, add `.cursor/rules/simple-code-writeing.mdc` (or equivalent name) under `F:\Codeing Project\odysseus-sysforge` with a non-empty `description` and `alwaysApply: false` (or chosen apply mode).
- [ ] Adapt any globs from `SysForge/**/*.cs` / `SysForge/**/*.axaml` to Odyssey paths (e.g. Python/Flask/static) if using path-based apply.
- [ ] Confirm rule is discoverable without `@`-mention (description present) — currently **no** `.cursor/rules/` in odysseus-sysforge.
- [ ] No product/runtime port from this chat (desktop Avalonia/C# code was not touched).

# Confidence

**High** for chat content (4-line transcript, single topic, no subagents). **Low product-migration impact** — Cursor IDE rule metadata only; nothing to port into app features, routes, or DB.
