# Chat review: 11201750-99af-446f-bce3-270d0cb387a6

## Title/topic

MCP toolchain adoption for SysForge work (Docker MCP `default` profile, Sequential Thinking usage), mapped onto open bugs and next plans from prior chats.

- Project: SysForge (Avalonia desktop)
- Transcript: `...\agent-transcripts\11201750-99af-446f-bce3-270d0cb387a6\11201750-99af-446f-bce3-270d0cb387a6.jsonl`
- Sibling subagent `*.jsonl`: none
- Related prior chats cited: [MCP Docker setup](f75b5117-f43b-4d14-91ed-5ec2c98dece5), [Backup & restore](55127af8-5c1c-45ed-8f64-b1558712f3cf)
- Product code changed in this chat: **none** (advisory / tooling only)

## Features

Discussed as product surface (not built in this chat):

| Area | Concrete names | Notes from chat |
|------|----------------|-----------------|
| Client search / Lucene | `SearchService`, `RebuildSearchIndexAsync`, Lucene client index | Clients indexed; parts still SQL `LIKE` |
| JSON import / export | `BackupService`, `sample-export.json`, `SysForge.Tests/TestData/sample-export.json` | Import must reindex; test fixtures include Walsh phone, Jenkins email, search terms `Maria`, `Chen`, `(555) 123-4567` |
| Backup / restore | `BackupService.cs`, `BackupServiceTests.cs`, Q-05 checklist | Backup → restore → export DB→JSON → re-import; duplicate detection |
| Diagnostics vs search UI | Diagnostics (`GetAllClientsAsync`), Client Dashboard + Invoice Calculator search | Same data, different read paths (DB vs Lucene) |
| Draft workflow tests | `DraftWorkflowTests` | Date-sensitive assertions |
| Async data layer | APP-07, `ClientService` Phase 1 | Keep sync methods; no ViewModel changes in Phase 1 |
| Navigation lifecycle | `CurrentView`, `MainWindowViewModel`, `state-and-event-lifecycle` rule | Double-assign same reference must not unsubscribe handlers |

MCP tooling inventory (not product features): MCP_DOCKER profile `default` (Desktop Commander, Context7, DuckDuckGo, Memory, Sequential Thinking, Task Orchestrator not exposed); also `git`, `json-query-mcp`, `mcp-obsidian`; Cursor `cursor-ide-browser` (low value for Avalonia).

## Issues/bugs

| ID / name | Status in chat | Detail to preserve |
|-----------|----------------|--------------------|
| **BUG-018** | Code fix claimed (`RebuildSearchIndexAsync` after import); **manual UI QA still open** | Imported clients appear in Diagnostics but not Client Dashboard / Invoice Calculator search. Hypotheses: stale Lucene index, wrong index path vs DB path, rebuild on empty set / transaction timing, `IsDeleted` / `IsIncomplete` filters. |
| **BUG-015** | Open at chat time | ~10 failing unit tests (79 pass / 10 fail); mostly draft date drift (`05/19/2026` vs `05/20/2026` in `DraftWorkflowTests`) |
| Desktop Commander mount gap | Config | Container mounted `F:\DMing-DND\Lunareth_DM_DND` only; needs `F:\Codeing Project\SysForge` for repo/DB access |
| Task Orchestrator | Present in Docker profile, **not exposed in Cursor** | Possible tool-count / gateway exposure limit |
| Uncommitted local work (at chat time) | Git status called out | `BackupService.cs`, `BackupServiceTests.cs`, `sample-export.json`, plan doc updates — verify whether these landed before porting |

Plan docs referenced: `Plans/Open-Bugs-And-Investigations.md`, `Plans/Post-Implementation-Improvements.md`, `Plans/README.md`.

## UX

- After JSON import, search in **Client Dashboard** and **Invoice Calculator** must find the same clients Diagnostics shows.
- Manual verify for BUG-018: import `sample-export.json` → search `Maria`, `Chen`, `(555) 123-4567`.
- Q-05: backup, restore, export DB file → JSON, re-import, duplicate detection.
- Import conflict / merge UI called out as deferred design (overwrite vs merge) — use structured planning before coding.
- `cursor-ide-browser` not useful for Avalonia desktop UX testing.

## Plans/deferred

| Item | Source | Scope |
|------|--------|--------|
| **APP-07** async DB migration | `Post-Implementation-Improvements.md` | Phase 1: `ClientService` async methods only; keep sync callers; then ViewModels / draft I/O / cancellation later |
| Lucene for **parts** search | Plans | Still SQL `LIKE`; compare extending `SearchService` vs separate `PartSearchService` |
| Q-05 import conflict / merge UI | Deferred | Design tradeoffs before implementation |
| Seed Memory MCP with project conventions | Suggested | Path, `%LOCALAPPDATA%\SysForge\sysforge.db`, never edit applied migrations, BUG-018/015, `CurrentView` lifecycle |
| Profile choice `default` vs `ai_coding` | Ops | `ai_coding` = Context7 + Sequential Thinking only (faster startup) |

## Odyssey checklist

Port / verify in `F:\Codeing Project\odysseus-sysforge` (and SysForge add-on paths under research docs):

- [ ] **Post-import search consistency (BUG-018 parity)** — Any JSON/DB import path for clients must refresh the search backend (FTS5 / Lucene / Odysseus search). Align with `Sysforge research/features/client-search-lucene.md` risk: reindex after bulk import.
- [ ] **Diagnostics vs typeahead parity** — Clients visible in diagnostics/list endpoints must appear in `/clients/search` (or FTS equivalent) used by dashboard + invoice calculator UIs.
- [ ] **Soft-delete / incomplete filters** — Port `IsDeleted` / `IsIncomplete` semantics into search queries so filtered-out rows do not look like “import failed.”
- [ ] **BackupService import → index hook** — When porting `BackupService` (`Sysforge research/features/settings-backup.md`, `services/sysforge/backup.py` / `routes/backup_routes.py`), wire reindex after client write/import; do not assume desktop Lucene path copy (`00-overview.md`: greenfield DB, no Lucene index copy).
- [ ] **Q-05-style backup round-trip** — Backup ZIP ↔ restore ↔ JSON export/import ↔ duplicate detection against Odysseus backup format (`docs/backup-restore.md`).
- [ ] **sample-export.json fixture** — Ensure test/fixture clients (Maria/Chen/Walsh/Jenkins phone patterns) exist for import+search regression tests in the add-on.
- [ ] **Draft date stability (BUG-015 class)** — Draft/invoice date assertions must not depend on “today”; use fixed dates in `DraftWorkflowTests` equivalents / Python tests.
- [ ] **APP-07 async pattern** — Prefer async service APIs in Python from the start (`ClientService`); avoid a second sync→async migration.
- [ ] **Parts search** — Keep SQL/FTS decision explicit; Lucene parts still deferred in desktop; FTS5 recommended for MVP (`client-search-lucene.md`, `parts-catalog.md`).
- [ ] **Navigation / event lifecycle** — If web UI has “active view” subscription patterns analogous to `CurrentView`, avoid unsubscribe-on-same-reference (desktop `state-and-event-lifecycle` rule).
- [ ] **Confirm BackupService fix landed in SysForge** before treating BUG-018 as closed for port assumptions; this chat did not verify commit state.

## Confidence

**Medium–high for tooling narrative; medium for product status.**

- Transcript is short and complete; no sibling subagents; no product diffs in-session.
- Open-bug statuses and uncommitted-file list are **secondhand** (from prior chats + `dotnet test` snapshot at that time); re-verify in SysForge git history and `Open-Bugs-And-Investigations.md` before treating BUG-018/015 as current truth.
- Odyssey checklist items are migration-relevant inferences from discussed SysForge behavior, not work done in this chat.
)
