# Chat review: c6ee4808-8cee-4854-b85a-331c23103080

Source: SysForge agent transcript (~413KB, 221 JSONL lines). Sibling subagents: `0f08b19f` (invoice/estimate gap analysis), `7a288724` (nav + Projects hub wiring guide). Branch throughout: `cursor/projects-plan-7229` (cloud plan doc origin; local implementation).

## Title/topic

**Projects and work orders (P1–P3)** end-to-end: migrations, services, calculator acceptance, Projects hub, project detail shell; then sandbox layout experiments culminating in a **four-column Project view** (user wireframe) with header project picker. Screw-map annotation UI is present on the branch by session end (used from the four-column view); plan phases P4–P7 stay deferred.

## Features

### P1–P3 core (shipped and pushed as `80622cc`)

**Database (migrations `0016`–`0018`)**
- **WorkOrders** + **WorkOrderInvoices** — group estimates after client acceptance
- **InvoiceDevices** — named devices on estimates
- **Projects**, **ProjectParts**, **ProjectNotes**, **ProjectAttachments**

**Services / models**
- `WorkOrderService` — create from accepted estimate, link invoices, search, hub queries
- `ProjectService` — create, notes, parts, photos, search, active projects; later `GetRecentProjectsAsync`
- `InvoiceService` — device CRUD + `SaveInvoiceDevicesAsync`
- Models: `WorkOrder`, `InvoiceDevice`, `Project`, `LinkedInvoiceRow`
- Settings: `ProjectsConfig` / “Include archived in search” in `config.json`

**UI**
- **Invoice calculator** — device rows; **Mark client accepted** (Option C dialog; optional bump to Invoiced); open work order; create/open project per device
- `ClientAcceptanceDialog`, `CreateProjectDialog`
- **Work order detail** — linked invoices
- **Project detail** (production scroll layout) — device fields, plain notes, parts, before/after photos, screw-map **placeholder** at P1–P3 ship
- **Projects hub** dashboard card — active projects, estimates not accepted, missing projects, search
- **Client dashboard Classic** — Projects column bound to `ClientProjects` (was “Coming later”)
- Navigation + DI for hub / WO / project detail

**Tests**
- `WorkOrderServiceTests` (3), `ProjectServiceTests` (3) — reported passing at P1–P3 completion

**Docs updated in-repo**
- `Plans/README.md`, Feature-Plans README, `Database-Design-Document.md`, `Invoice-Roadmap.md`, feature plan status

### Layout prototyping (session mid-game)

1. **Three sandbox layout cards** (Tabs / Split / Timeline) — built, then **removed** before screw-map work (`13f27fa` only updated Sandbox README; prototype files never reached remote).
2. **Four-column Project view** (user design PNG) as separate dashboard card **“Project view (new layout)”** — does **not** replace production `ProjectDetailView` / Projects hub path.
   - Files: `ProjectDetailFourColumnView(.axaml/.cs)`, `ProjectDetailFourColumnViewModel`
   - Columns: Notes | Parts/invoice | Before/After + screw-map preview | Device details
   - Extends production project VM; screw-map open → annotation UI; SW projects hide screw map (`IsScrewMapEligible`)
3. **Project picker UX**
   - First: empty until Search → fixed with `GetRecentProjectsAsync` (up to 30), load on open / `OnViewActivated`, search includes title
   - Then: moved into main window header via `ProjectPickerHeaderBar` (max **3** recents / search chips) — commit `0ee61e3`

### Screw map (on branch by end; used from four-column view)

Chat’s final push notes prior commits already included screw-map service/migrations/annotation UI (`58914c7`, `c0abccd`, `4c68145` territory). Four-column col 3 wires start/upload/open annotation; production scroll view still holds richer library reuse / lock / publish until v2 promoted.

### Subagent findings (pre-implementation baselines)

- Estimates = same `Invoice` row with `Status == Estimate`; no client-acceptance UI; `UpdateInvoiceStatusAsync` / `FinalizeInvoiceAsync` unused from UI.
- Classic dashboard Projects column was static placeholder; ViewLocator + dashboard-card + DI pattern documented for hub.

## Issues/bugs

| Item | Status in chat |
|------|----------------|
| User: “Mostly Works, some bugs occur” after P1–P3 | **Unspecified** — push first; never listed; still open as unknown QA debt |
| Four-column picker empty (no default recents) | **Fixed** — `GetRecentProjectsAsync` + load on open |
| Col 4 fixed width; notes not scrolling; device labels stacked | **Fixed** in wireframe audit pass |
| Screw map started with 0 photos → no upload path | **Fixed** — “Upload screw map photo” when `HasScrewMap` |
| Screw-map preview not refreshing after upload | **Fixed** — `ScrewMapImageRows.CollectionChanged` |
| Misleading “click to open” / “0 photos · Updated …” | **Fixed** |
| Tabs/Split/Timeline prototypes | Built then deleted; not a product bug |
| Local vs remote drift | Prototype deletes were local-only; four-column + header landed across several commits; agent left **uncommitted** local work (plan docs, invoice/draft tweaks, wireframe tweaks, `CreateProjectFromInvoiceService`, select-invoice dialogs) |
| App locking build output during iterates | Operational friction; restart app to verify |

## UX

- **Acceptance path:** save estimate → add devices → Mark client accepted → create project per device → Projects hub.
- **Two project UIs:** production scroll `ProjectDetailView` (hub) vs sandbox **four-column** card until new layout is promoted.
- Four-column matches user wireframe closely; intentional extras: header picker, status strip, Save notes, upload buttons, part-pictures stub.
- Header picker: compact search under back/title; 3 recent chips; frees workspace for four columns.
- Client dashboard: real Projects list in Classic layout (not sidebar-only).
- Settings toggle for including archived projects in search.

## Plans/deferred

From feature plan (explicit at P1–P3 ship):
- **P4** rich notes
- **P5** screw maps (annotation path partially present; full P5 / library / publish not closed in this chat)
- **P6** inventory
- **P7** release package

Other deferred / follow-ups called out:
- Promote four-column to replace production detail when “looks good”
- Hide picker when navigating with a known project ID from hub
- Part pictures beyond stub
- Screw-map library reuse / lock / publish stay on scroll view until v2
- Enumerate and fix the unspecified post-P1–P3 bugs
- Commit leftover local files if still needed
- Spec anchors: `Plans/Feature-Plans/Projects-and-Work-Orders-Plan.md`, `Plans/Product-Vision/Repair-Documentation-System.md`

## Odyssey checklist

Port targets for odysseus-sysforge (web); Avalonia AXAML → HTML/JS, same domain rules.

- [ ] **Schema:** WorkOrders + WorkOrderInvoices; InvoiceDevices; Projects + ProjectParts / Notes / Attachments (map SysForge `0016`–`0018` to Odyssey migrations / models).
- [ ] **WorkOrderService parity:** accept estimate → WO; link invoices; hub bucket queries; WO detail with linked invoices.
- [ ] **ProjectService parity:** CRUD, notes, parts, before/after photos, search, active list, recent projects (limit 3 for compact picker if used).
- [ ] **Invoice devices on calculator/estimate:** named device rows; persist with invoice save.
- [ ] **Client acceptance (Option C):** dialog; create WO; optional status → Invoiced; do not treat `IsFinalized`/`SentAt` alone as acceptance.
- [ ] **Create project per device** wizard/dialog from accepted estimate / WO.
- [ ] **Projects hub:** active / estimates not accepted / accepted missing projects; search; respect include-archived setting.
- [ ] **Client detail Projects column** (or equivalent) fed by client-scoped projects, not a placeholder.
- [ ] **Project workspace:** either port production scroll detail first, or adopt four-column layout as primary; keep dual-layout only if still A/B’ing.
- [ ] **Screw map:** start/upload/annotate; hide for SW-ineligible projects; fix dead-end (map exists, no photos) and preview refresh after upload.
- [ ] **Settings:** include archived in project search.
- [ ] **Tests:** WO + Project service suites mirroring SysForge tests.
- [ ] **Do not block on:** Tabs/Split/Timeline sandbox cards (removed); rich notes / inventory / release package (P4/P6/P7).
- [ ] **QA debt:** chase unspecified “mostly works” bugs against SysForge branch `cursor/projects-plan-7229` before calling Odyssey parity done.
- [ ] Cross-check SysForge paths: `WorkOrderService.cs`, `ProjectService.cs`, `InvoiceCalculatorViewModel` acceptance + devices, `ProjectsHubViewModel`, `ProjectDetailView` / `ProjectDetailFourColumnView*`, `ClientAcceptanceDialog`, migrations `0016`–`0018`.

## Confidence

**High** for P1–P3 feature inventory, deferred phases, and four-column / picker UX (assistant summaries + file Write list + commits `80622cc`, `13f27fa`, `0ee61e3`). **Medium** for exact screw-map migration IDs and full P5 completeness (chat uses screw-map APIs and cites earlier commits; this transcript does not narrate the full screw-map build). **Low** for the user’s unnamed post-ship bugs (acknowledged, never described). Subagents are strong pre-impl baselines, not post-ship verification.
