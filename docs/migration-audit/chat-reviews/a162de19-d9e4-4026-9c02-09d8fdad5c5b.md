# Chat review: a162de19-d9e4-4026-9c02-09d8fdad5c5b

## Title/topic

Deferred / “never worked on” backlog inventory across SysForge agent sessions and planning docs (no product code changes).

- Project: SysForge (Avalonia desktop)
- Transcript: `...\agent-transcripts\a162de19-d9e4-4026-9c02-09d8fdad5c5b\a162de19-d9e4-4026-9c02-09d8fdad5c5b.jsonl`
- Sibling subagent: `subagents\5418d0a4-5ab3-47ff-ad94-88a5f1656791.jsonl` (`ce-session-historian` style inventory; expanded the parent’s list)
- Related prior chats cited: [unimplemented plans audit](97aa3734-3500-4340-8c0a-2c82555c2b0c), [client dashboard & invoice / MRU deferral](6bba6919-90f5-4c2c-959c-b6fb279f5abd), [per-invoice JSON backup](f4de4b62-87a3-4f5f-b09f-09cc62baa5c8), [failed-fix / DebugLog audit](b89af01b-df9a-4314-9192-0f96200cf8e3)
- Docs scanned: `SysForge/Future-UI-Features.md`, `Plans/Post-Implementation-Improvements.md`, `Plans/TODO.md`, `Repair-Documentation-System.md`, `Roadmap.md` / `Invoice-Roadmap.md`, `SettingsService.cs` deferred comments
- Product code changed in this chat: **none** (read-only audit)

## Features

This chat did not ship features. It classified product surface as deferred, partial, or already shipped.

### Explicitly deferred by user (highest signal)

| Area | Concrete names | Notes from chat |
|------|----------------|-----------------|
| Client dashboard MRU | `LastInteractedAt`, `GetRecentClients`, `ClientDashboardViewModel`, `Future-UI-Features.md` | Default to last saved/edited/viewed client; show last **6** interacted clients in search before typing. User deferred in [6bba6919](6bba6919-90f5-4c2c-959c-b6fb279f5abd); still missing at audit time. |

### Documented backlog (chat claimed still missing)

| Area | Concrete names | Notes from chat |
|------|----------------|-----------------|
| Async DB layer | APP-07, `SearchClientsAsync` / `SaveInvoiceAsync`, `ClientService` / `InvoiceService` / `PartService` | Only connection helpers async; services sync |
| User backup & restore | Q-05, `BackupService`, Settings UI, scheduled backups | Distinct from per-invoice `InvoiceService.BackupExistingInvoice` / `InvoiceBackups/` |
| Duplicate detection + merge | DB-12, `FindPotentialDuplicates`, `FindClientsByPhone`, merge UI | Real-time warnings + future merge tool |
| Root README / doc index | DOC-01, DOC-02 | No root `README.md` |
| Repair documentation | `ScrewMap`, `RepairSession`, `Repair-Documentation-System.md` | Screw maps, repair timeline; doc-only at audit time |
| Invoice / product v2+ | `Roadmap.md`, `Invoice-Roadmap.md` | Suppliers UX, inventory, reporting, PDFs, payments |
| IT ops integrations | Wazuh, Snipe-IT, NetBox, n8n; `Use-Cases.md`, `Integrations.md` | Parallel product vision; not in desktop app |
| Lucene for parts | `PartService.SearchParts`, `SearchService` | Clients Lucene; parts SQL `LIKE` |
| Multi-instance coordination | Q-04 | Documented future goal only |
| Future UI Phase 2–3 | Favorites, card previews, keyboard shortcuts, breadcrumbs, status bar, plugins, themes, user profiles, sidebar↔dashboard sync | Phase 1 shell/toasts largely done |
| Config persistence gaps | `SettingsService`: `NavigationHistory`, `IsSidebarCollapsed`, `LastViewedSection` | Marked deferred in config schema |
| Placeholder plan doc | `Plans/Placeholder-Parts-Workflow-Plan.md` | Empty file; merge UI existed elsewhere |

### Partial at audit time

| Item | State |
|------|--------|
| Draft auto-delete (DRAFT-08) | `DraftService.DeleteOldDrafts()` exists; **not wired** to startup |
| Navigation history | In-session `NavigationHistory` works; **not persisted** across restarts |
| Per-invoice backup | JSON snapshot on invoice update; full backup/restore system separate |

### Verified shipped (do not treat as backlog)

| Item | Evidence in chat |
|------|------------------|
| Edit Client UI | Built in [6bba6919](6bba6919-90f5-4c2c-959c-b6fb279f5abd) after MRU deferred |
| Invoice naming + save flow | Same session |
| Finalized invoice price comparison | `InvoiceEditViewModel.ComparePricesToDatabase`, `HasPriceMismatch` |
| Client search: matches first, “Add new” last | `InvoiceCalculatorViewModel.UpdateClientSearchResults`; `Plans/TODO.md` stale |
| Debug instrumentation cleanup | `DebugLog` / `DebugSession` removed after [b89af01b](b89af01b-df9a-4314-9192-0f96200cf8e3) |

**Caveat from chat:** many `Plans/Implementation-Plan/Phase-*/README.md` files still said “Planned” while DI, cents, UTC, drafts, client dashboard, invoice edit were already in the app.

## Issues/bugs

No new runtime bugs filed in this chat. Process / staleness issues:

| Issue | Detail |
|-------|--------|
| Stale plan / TODO docs | Phase READMEs and `Plans/TODO.md` overstate unfinished work (e.g. client search reorder already done) |
| Session inventory scripts failed on Windows | `ce-session-inventory` / `python3` path failures; fallback was transcript grep + `97aa3734` / `6bba6919` deep-dives |
| Empty plan file | `Placeholder-Parts-Workflow-Plan.md` abandoned while feature lived in code |
| Chat-era status drift | Later SysForge work (and `docs/research/sysforge/01-feature-inventory.md`) mark several “still missing” items as Done or In progress — treat this transcript as a **snapshot**, not current SysForge truth |

## UX

User- and doc-facing UX called out as unfinished or deferred:

- **Client MRU:** opening Client Dashboard should land on last-used client; empty search should show recent-6 before typing.
- **Future-UI track:** favorites, card previews, keyboard shortcuts, breadcrumbs, status bar, sidebar collapse + last-viewed section persistence, themes, profiles, notification history, background preloading.
- **Navigation:** back stack useful in-session; users lose history after restart without config persistence.
- **Drafts:** retention config without startup auto-delete leaves old drafts around.
- **Backup UX:** per-invoice JSON is not a user-facing backup/restore product.
- **Duplicates:** no create-time warning / merge path for accidental clients.
- **Parts search:** SQL `LIKE` ranking vs Lucene SKU-first (clients already Lucene).
- Agent recommendation: smallest high-value next pick = **client dashboard MRU** (spec already in `Future-UI-Features.md`).

## Plans/deferred

Top impact ranking from parent + subagent:

1. Repair documentation / screw maps (largest standalone spec at chat time)
2. Invoice/product v2+ (suppliers, inventory, merge, reporting, PDFs)
3. IT ops integrations (Wazuh / Snipe-IT / NetBox / n8n)
4. `Future-UI-Features.md` Phase 2–3
5. `Post-Implementation-Improvements.md` — APP-07 async DB, Q-05 backup/restore, DB-12 duplicates+merge, DOC-01/02 README

Also deferred: Q-04 multi-instance; DRAFT-08 startup wiring; Lucene parts; config fields `NavigationHistory` / `IsSidebarCollapsed` / `LastViewedSection`; empty Placeholder-Parts plan doc.

## Odyssey checklist

Port / verify in `F:\Codeing Project\odysseus-sysforge` (plugin + research docs). Status notes reflect **today’s tree**, not chat-era SysForge alone.

- [ ] **Client MRU / recent-6** — Still **Planned/deferred** in `docs/research/sysforge/features/client-dashboard.md` and `01-feature-inventory.md`. No `LastInteractedAt` / `GetRecentClients` in `integrations/sysforge/` (plugin is status + placeholder UI only).
- [ ] **Client dashboard + invoice calculator UIs** — Port behind optional plugin (`integrations/sysforge/`, `manifest.json` id `sysforge`, Settings install). Placeholder: `integrations/sysforge/static/js/index.js`.
- [ ] **Duplicate detection** — Research marks `FindPotentialDuplicatesAsync` **Done** in desktop; merge tool still deferred. Port detection into add-on create-client path; design merge UX separately (DB-12).
- [ ] **Price compare on finalized invoices** — Research: `InvoiceEditViewModel.ComparePricesToDatabase` / `HasPriceMismatch` **Done** in desktop; ensure Odyssey invoice-edit port keeps catalog mismatch UX.
- [ ] **Client search order** — “Matches first, Add new last” already correct in desktop `UpdateClientSearchResults`; do not reintroduce stale TODO behavior in web typeahead.
- [ ] **Draft retention / DeleteOldDrafts** — Research still **Partial** (method exists, not on startup). Plugin already creates `drafts/` (`install.py`); wire retention cron/task when drafts land.
- [ ] **Navigation history persistence** — Research shell uses in-session stack (`NavigationHistory`, `06-frontend-routing.md` back stack max 20). Decide: session-only vs persisted (desktop config deferred).
- [ ] **Parts search** — Keep SQL/FTS decision explicit; Lucene parts still Planned in inventory; FTS5 preferred for MVP over copying Lucene.
- [ ] **Async services** — Chat said APP-07 missing; research inventory now **Done** on desktop. Prefer async Python APIs from day one in `services/sysforge/*` (avoid sync→async redo).
- [ ] **User backup & restore (Q-05)** — Chat said missing; research marks desktop `BackupService` / `ScheduledBackupService` **Done**. Align plugin backups with Odysseus ZIP format (`docs/backup-restore.md`); plugin already mkdirs `backups/`. Distinct from per-invoice JSON snapshots.
- [ ] **Screw maps / repair docs** — Chat said zero code; later inventory + other chat reviews show migrations `0019`–`0021`, `ScrewMapService`, annotation UI **In progress**. Port from current SysForge, not this chat’s “doc-only” claim. See also review `150e09d0-…`.
- [ ] **Future-UI Phase 2–3** — Documented in `docs/research/sysforge/features/future-ui-backlog.md`: defer favorites/shortcuts/plugins/themes to add-on v1.1+; reuse Odysseus theming (`--section-accent`) and existing shortcut settings; avoid duplicate plugin models vs Odysseus MCP/skills.
- [ ] **v2+ roadmap / IT integrations** — Keep out of MVP Business plugin scope (suppliers inventory, PDFs, payments, Wazuh/Snipe-IT/NetBox/n8n).
- [ ] **Sidebar collapse / last-viewed section** — Desktop config deferred; web can use localStorage or settings only if needed after core flows.
- [ ] **Do not trust Phase README “Planned” labels** — Cross-check `docs/research/sysforge/01-feature-inventory.md` and live SysForge before prioritizing ports.

## Confidence

**High for what this chat concluded at the time; medium for current SysForge/Odyssey truth.**

- Parent + sibling transcripts are complete and agree on the deferred list and citations (`97aa3734`, `6bba6919`).
- Chat verified several items against SysForge code then; it did not touch odysseus-sysforge.
- Desktop status has moved since (backup, async, duplicates, screw maps per research inventory). Odyssey checklist above reconciles chat claims with `docs/research/sysforge/` and the thin `integrations/sysforge/` plugin scaffold.
- Confidence on Odyssey implementation is **low/none** for domain modules: only plugin install/status/placeholder panel exists today.
