# Title/topic

Troubleshoot Cursor **MCP_DOCKER** (Docker MCP Toolkit gateway) connection failures in the SysForge workspace.

Source: [f75b5117](f75b5117-f43b-4d14-91ed-5ec2c98dece5) — SysForge agent chat. No sibling subagent `*.jsonl` in the transcript folder (10 lines; single topic).

# Features

- No SysForge product features discussed or implemented.
- Dev-environment only: Docker MCP gateway with profile `ai_coding`.
- Profile servers named: `context7`, `sequentialthinking`.
- Cursor config surface: `~/.cursor/mcp.json` entry `MCP_DOCKER` → `docker mcp gateway run --profile ai_coding`.
- Agent actions: verify Docker Desktop running, pull MCP images by digest, dry-run gateway, confirm warm startup ~20s (cold ~80s).

# Issues/bugs

Three connection failures diagnosed from MCP logs (not app bugs):

1. **Docker Desktop not running** → `MCP error -32000: Connection closed`.
2. **Handshake timeout (~60s)** while first-run image pulls took **2m18s–2m40s** → `MCP error -32001: Request timed out`.
3. **Profile churn mid-connect** → repeated `DeleteClient` with `server_disabled` / `config_server_removed` interrupting startup.

Non-fatal noise called out:

- Docker MCP plugin logs startup lines (`Reading profile configuration...`, `Loading 1 catalog(s)...`) at error level.
- `Failed to connect to OAuth notifications` — irrelevant for `context7` / `sequentialthinking` (no OAuth secrets needed).

# UX

- N/A for SysForge or odysseus-sysforge product UI.
- Agent/dev UX only: start Docker before enabling MCP; pre-warm gateway; toggle MCP_DOCKER off/on; avoid editing Docker MCP Toolkit servers while Cursor connects; optionally shrink profile to one server for isolation.

# Plans/deferred

- Optional: keep `ai_coding` lean (2 servers) for faster MCP startup.
- Optional: if reconnect still fails, enable only `sequentialthinking`, then grow the profile; pre-pull images before adding servers.
- No product backlog, feature deferrals, or SysForge→web port plans in this chat.

# Odyssey checklist

**Product migration impact: none.** This chat is IDE/Docker tooling in the SysForge Cursor project folder.

- [ ] No app feature, route, DB schema, or Avalonia→web UX to port from this transcript.
- [ ] Skip treating MCP_DOCKER / `context7` / `sequentialthinking` as odysseus-sysforge product requirements.
- [ ] Optional (dev-env only): if Odyssey agents use the same Docker MCP gateway, ensure Docker Desktop is up, images pre-pulled, and profile stable before Cursor MCP connect — same ops notes as SysForge workspace.
- [ ] Do not add Docker MCP profile/config into odysseus-sysforge product code or plugin packaging based on this chat.

# Confidence

**High** for chat content (short, single troubleshooting thread, no subagents, clear diagnosis).  
**Negligible product-migration value** — zero SysForge domain features, bugs, or UX for odysseus-sysforge parity checks.
