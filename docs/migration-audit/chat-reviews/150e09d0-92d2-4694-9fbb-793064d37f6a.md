# Chat review: 150e09d0-92d2-4694-9fbb-793064d37f6a

**Project:** SysForge  
**Source:** `C:\Users\User\.cursor\projects\f-Codeing-Project-SysForge\agent-transcripts\150e09d0-92d2-4694-9fbb-793064d37f6a\`  
**Sibling subagents:** none  
**Nature:** Status recon + UI-design handoff advice (no code changes in-session)

## Title/topic

Screw map progress check (S0–S5) and how to hand rough UI designs to the agent (Figma / HTML / Excalidraw / marked-up screenshots + Sandbox workflow).

## Features

Claimed present in SysForge desktop (chat inventory; verify against current tree):

- **S0+S1 core:** migrations `0019`–`0021` (`ScrewMaps`, images, markers, library sets); `ScrewMapService` (create, upload, markers, lock checks); project detail start map / desktop photo upload / image list / open annotation; annotation View/Edit, double-click screws, right-click notes, drag markers, label/notes/warning, delete markers; HW + Other category gate; `ScrewMapServiceTests` (17 passing at chat time).
- **S2 measurements (partial):** length, shaft diameter, head diameter on screw detail panel → DB; head type column exists without UI; no zoom/pan; no photo delete/reorder.
- **S3 reverse lookup:** “Find hole by measurements” panel; fuzzy match on current map; jump to image/screw.
- **S4 lock + library:** auto-lock on project status `FinishedWaitingDropOff` or `WaitingOnPayment`; manual “Lock screw map now”; publish to library (title + tags) when locked; browse/reuse library with provenance labels on reused photos.
- **Key types/paths cited:** `ScrewMapService`, `ScrewMapAnnotationViewModel`, `ProjectDetailView` / `ProjectDetailViewModel`, `ScrewMapServiceTests`, plans `Repair-Documentation-System.md`, `Screw-Map-S5-Mobile-Companion-Plan.md`, `Screw-Map-Decisions-Questionnaire.md`, `Product-Decisions-Questionnaire.md` Part IV, `Projects-and-Work-Orders-Plan.md`.

## Issues/bugs

- Upload / lock / publish / save failures can fail silently (logged only; no toast/inline UI) — questionnaire item **A7**.
- Head type: DB ready, annotation panel does not expose picker.
- No delete/reorder for photos before lock.
- No zoom/pan on annotation canvas (painful on dense board shots).
- Product decisions still open in `Product-Decisions-Questionnaire.md` Part IV (lock rules, unlock policy, etc.).
- Chat noted screw map work may be uncommitted / docs stale (`Repair-Documentation-System.md` still said “S0+S1 only” while code had S3/S4) — re-check current git state before port assumptions.
- Research doc `docs/research/sysforge/features/screw-maps.md` in Odyssey still says “S0+S1 in progress”; chat claims S3/S4 built — inventory drift risk.

## UX

- Annotation: big image area + right detail/sidebar + bottom lookup strip (suggested Excalidraw layout).
- Modes/states to preserve: empty map, editing, locked read-only, lookup results.
- Behavior notes: upload only when unlocked; double-click adds screw; orange = warning screw (example handoff note).
- Design handoff path recommended: Excalidraw wireframe or marked-up screenshot → build in `SysForge/Views/Sandbox/` first → merge into `ProjectDetailView` / `ScrewMapAnnotationView`.
- Target frame is desktop (e.g. ~1280×800), not phone-sized mocks, for Avalonia windows.
- Screens to design: project detail screw map section, annotation view, library picker, (later) phone upload page.

## Plans/deferred

- Finish **S2**: head type dropdown, optional zoom/pan, delete/reorder images (before lock).
- Surface errors in UI (toast/inline).
- Answer Part IV product decisions (lock timing, unlock allowed?).
- Commit S0–S4 desktop slice as milestone.
- **S5 mobile companion** not started: LAN browser + QR upload per `Screw-Map-S5-Mobile-Companion-Plan.md`.
- Manual smoke path suggested: HW project → start map → upload 2 photos → annotate → measurements → lookup → lock → publish → new project → reuse from library.
- Odyssey research already suggests deferring mobile companion if web UI covers tablet use.

## Odyssey checklist

Port / verify in `F:\Codeing Project\odysseus-sysforge` (do not assume Avalonia 1:1):

- [ ] Schema parity for migrations `0019`–`0021` (maps, images, markers, library sets) + head-type column even if UI incomplete.
- [ ] `ScrewMapService`-equivalent API: create map, upload images, markers CRUD, lock checks, publish/reuse library.
- [ ] Project detail screw-map section: start map, upload, image list, open annotation.
- [ ] Annotation UI on HTML canvas/SVG: View/Edit, add/drag markers, notes/warning, delete markers; HW/Other gate.
- [ ] Measurement fields: length, shaft diameter, head diameter; plan head-type picker.
- [ ] Reverse lookup panel (“Find hole by measurements”) + jump to match.
- [ ] Lock: auto on `FinishedWaitingDropOff` / `WaitingOnPayment`; manual lock; publish when locked; library browse/reuse + provenance labels.
- [ ] User-visible errors on upload/lock/save/publish (fix silent log-only failures).
- [ ] Photo delete/reorder before lock; zoom/pan on annotation (S2 gaps).
- [ ] Align `docs/research/sysforge/features/screw-maps.md` status with real S0–S4 scope; keep S5 deferred unless shop needs LAN/QR phone upload.
- [ ] Resolve or document Part IV lock/unlock product decisions before hardening lock behavior.
- [ ] Smoke the chat’s end-to-end flow in web UI once ported.

## Confidence

**Medium-high** for feature inventory and gaps (agent inspected concrete files/tests and ran `ScrewMap` filter). **Medium** for “done vs partial” labels vs today’s SysForge tree (chat is a recon snapshot; commit/doc drift called out in-session). **Low** for Odyssey implementation status (this chat did not touch odysseus-sysforge). No sibling subagent transcripts.
)
