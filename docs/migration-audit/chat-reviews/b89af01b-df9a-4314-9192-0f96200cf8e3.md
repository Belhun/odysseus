# Title/topic

Failed-fix leftovers audit and DebugLog cleanup (SysForge, May 13, 2026)

Parent chat: [Failed-fix leftovers audit](b89af01b-df9a-4314-9192-0f96200cf8e3)  
Sibling subagent: `subagents/7eeac4a5-7daf-4692-bf62-cfe0f1182994.jsonl` (readonly audit report)  
Related earlier chats cited in audit: [Invoice names, diagnostics, DataGrid theme](6bba6919-90f5-4c2c-959c-b6fb279f5abd), [Phase 9 CHECK constraints](f4de4b62-87a3-4f5f-b09f-09cc62baa5c8)

Session flow: (1) audit recent commits + chats for failed-fix leftovers; (2) dependency/impact pass if those leftovers were removed; (3) remove `DebugLog` + call sites and list manual regression checks.

---

# Features

Product surfaces and patterns this chat treats as real (keep / port), not debug scaffolding:

- **Diagnostics screen** — in-app DB path, size, client/invoice grids, Refresh. Product replacement for throwaway `InspectLiveDb`.
- **`HasInvoices` boolean** on client dashboard — drives DataGrid vs empty-state visibility (replaces fragile `ClientInvoices.Count` + converter binding).
- **Edit invoice via calculator** — `NavigateToInvoiceCalculatorEditAsync` + `LoadWithInvoice`; not the old `InvoiceEditView` route.
- **View invoice** — `NavigateToInvoiceViewerAsync` from dashboard.
- **`NotifyViewActivated` + dashboard refresh** — refresh cached `ClientDashboardViewModel` when returning so new invoices show without re-picking the client.
- **DataGrid Fluent theme `StyleInclude` in `App.axaml`** — actual root-cause fix for blank DataGrids (session `6bba6919`).
- **Phase 9 CHECK / enum handling** (cleared as working) — manual `.ToString()` for `DiscountType` / `ItemType`; empty item lists allowed on edit.
- **`CountToVisibilityConverter`** — still used by Placeholder Merge; keep (unlike dead `CountToHasItemsConverter`).

Done in this chat (SysForge desktop):

- Removed `SysForge/Debug/DebugLog.cs` and all `SysForge.DebugSession.DebugLog` call sites.
- Stripped Diagnostics DataGrid `Loaded` / `AttachedToVisualTree` handlers, grid `Name=` attrs, and `PRAGMA table_info(Invoices)` probe in `DiagnosticsViewModel.Refresh`.
- Simplified `InvoiceViewerView` / `DiagnosticsView` code-behind to plain shells.
- Dropped `debug-6bba69.log` from `.gitignore` (left `.cursor/debug.log`).

---

# Issues/bugs

Root issue this cleanup sat on top of (from cited May 12 chat):

- **Blank DataGrids** — data was present; missing Avalonia DataGrid Fluent theme styles. Layout/converter/refresh attempts did not fix it.

Failed-fix leftovers identified (audit + dependency pass):

| Leftover | Status in this chat | Removal impact |
|----------|---------------------|----------------|
| `DebugLog` + ~30 `#region agent log` sites | **Removed** | No correctness deps; less CPU/I/O; `ILogger` remains |
| Diagnostics handlers + schema PRAGMA | **Removed** with DebugLog | Diagnostics UI/data load unchanged |
| `CountToHasItemsConverter` | Still present; no XAML consumers | Safe remove; keep sibling `CountToVisibilityConverter` |
| `InspectLiveDb` sandbox Fact | Still present; no app callers | Optional test only; early-returns if DB missing |
| `InvoiceEditView` / VM / `NavigateToInvoiceEditAsync` | Still present; **no production callers** | Remove DI + title branch + files together; port “Update to DB price” first if wanted |
| `RefreshClientInvoices` shim | Still present; 2 `NavigationService` callers | Call `RefreshInvoices()` then delete shim |

Other notes:

- Hardcoded log path `f:\Codeing Project\SysForge\...` made agent NDJSON logs machine-local and silent elsewhere.
- Build after DebugLog removal succeeded; existing test failures (`JsonSchemaSyncTests`, `MigrationsTests`, `DraftServiceTests`, etc.) called unrelated.
- Agent briefly removed then **restored** a separate `.cursor\debug.log` `File.AppendAllText` block in `AddItemWithName` (out of DebugLog scope).

---

# UX

Manual checks listed after DebugLog removal (paths that used to hit agent logs):

- Client dashboard: invoice list + empty state (`HasInvoices`); Refresh / Edit / View / New invoice; return from calculator and confirm list updates without re-selecting client.
- Invoice calculator: add items, focus/tab behavior; Edit loads via `LoadWithInvoice`; prices restore after load (`OnViewLoaded` / dispatcher kept).
- Invoice viewer: View from dashboard; data and title.
- Diagnostics: path/size/counts and both grids; Refresh reloads.
- Navigation chrome: calculator edit, viewer, dashboard titles/content switch correctly.

UX product notes for migration:

- Empty invoice list must use an explicit boolean (or equivalent), not `Collection.Count` through a converter Avalonia can miss.
- Blank grids with “data in DB” → check theme/styles before adding more refresh/probe code.
- Old edit screen had per-item “Update to DB price” style UI; calculator path may not expose that unless ported.

---

# Plans/deferred

Explicitly not done in this chat (user only approved DebugLog cleanup):

1. Remove remaining `.cursor\debug.log` `File.AppendAllText` instrumentation in `InvoiceCalculatorViewModel` / `InvoiceCalculatorView.axaml.cs` (agent offered next).
2. Delete dead `CountToHasItemsConverter`.
3. Delete `InspectLiveDb.cs` (Diagnostics replaces it).
4. Remove or fully retire `InvoiceEditView` / `InvoiceEditViewModel` / `NavigateToInvoiceEditAsync` / DI / title mapping; port price-mismatch UI to calculator first if still wanted.
5. Replace `RefreshClientInvoices` shim with direct `RefreshInvoices()` at two `NavigationService` sites.
6. `DatabaseDiagnostics.DetectMixedTimezones` — planned on-demand tool (Phase 4), not a failed fix; out of scope here.

---

# Odyssey checklist

Use when verifying or porting behavior into **odysseus-sysforge**:

- [ ] **No agent NDJSON / hardcoded debug writers** on hot paths (navigation, load, refresh). Prefer normal app logging only.
- [ ] **No leftover `#region agent log` / session-id log files** (`debug-6bba69` style) in shipped code or ignore rules unless still intentional.
- [ ] **Invoice edit** goes through calculator-style load (`LoadWithInvoice` equivalent), not a separate dead edit route.
- [ ] If calculator edit lacks **price-mismatch / “Update to DB price”**, decide explicitly: port or drop.
- [ ] **Dashboard empty vs list** uses a reliable flag (like `HasInvoices`), not Count-via-converter.
- [ ] **Return-to-dashboard refresh** exists for cached views after create/edit (NotifyViewActivated-style).
- [ ] **Diagnostics** (or equivalent) is a real UI feature; do not rely on sandbox one-off DB dump tests.
- [ ] If Avalonia/DataGrid UI: confirm **DataGrid theme/styles** are included (blank grids were a theme miss, not missing data).
- [ ] CHECK / enum string persistence and **empty line-item edits** match Phase 9 behavior if invoice save is ported.
- [ ] Do not port unused converters (`CountToHasItemsConverter`) or dead edit screens “just because they exist in SysForge.”
- [ ] After any debug-instrumentation strip: run the manual UX checklist above on web equivalents (dashboard list, edit, view, diagnostics/settings DB info if any).

---

# Confidence

**High** for: DebugLog was pure instrumentation; Diagnostics product Refresh vs probe split; Edit routes through calculator; `CountToHasItemsConverter` unused; dependency table for removal impact.

**Medium** for: whether SysForge still contains items 2–6 after later chats (this session only removed DebugLog + related Diagnostics wiring); whether Odyssey already mirrors calculator-edit vs old edit; exact state of remaining `.cursor\debug.log` Append blocks.

**Lower** for: full Odyssey feature parity claims (this chat did not touch odysseus-sysforge); whether “Update to DB price” is still a product requirement.

Sources: full parent transcript (51 lines) + full subagent audit report (43 lines). No product code modified for this review note.
)
