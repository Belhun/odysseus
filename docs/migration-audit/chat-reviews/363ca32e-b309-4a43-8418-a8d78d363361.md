# Chat review: 363ca32e-b309-4a43-8418-a8d78d363361

**Source:** SysForge agent transcript  
**UUID:** `363ca32e-b309-4a43-8418-a8d78d363361`  
**Sibling subagents:** none in folder

## Title/topic

Push uncommitted SysForge work; promote **four-column project detail** to production UI and ship to `origin`.

## Features

- **Production project detail UI** is `ProjectDetailFourColumnView` / `ProjectDetailFourColumnViewModel` (under `SysForge/Views/Sandbox/` and `SysForge/ViewModels/Sandbox/`, but wired as the live route).
- **Navigation:** all project-detail navigation goes through `ProjectDetailFourColumnView` via `NavigationService` (`NavigateToProjectDetail*`, invoice return context, `PrepareProjectDetailViewModelAsync`).
- **Legacy scroll UI removed:** `ProjectDetailView` (scroll layout) dropped as the production surface; `ProjectDetailViewModel` remains as the base class for `ProjectDetailFourColumnViewModel`.
- **Avalonia designer support:** parameterless dialog constructors added (commit summary; exact dialog types not listed in transcript).
- **Git ship:** branch `cursor/projects-plan-7229` → `origin` (https://github.com/Belhun/SysForge.git); commit `d45fa40` — `feat(projects): make four-column view the production project UI` (12 files).

## Issues/bugs

- None reported in this chat.
- Pre-push state: branch already tracked/synced with origin, but **12 local uncommitted files** blocked a clean push until commit `d45fa40`.

## UX

- Project detail UX standard is the **four-column** layout (not the older single-scroll `ProjectDetailView`).
- Related shell pieces present in repo at review time: `ProjectPickerHeaderBar`, before/after photo paths, screw-map preview (`FirstBeforePhotoPath`, `FirstAfterPhotoPath`, `ScrewMapPreviewPath` on `ProjectDetailFourColumnViewModel`).

## Plans/deferred

- This chat is ops-only (commit + push). No new feature plans or deferred items discussed.
- Broader projects work remains on branch name `cursor/projects-plan-7229` (plan context outside this transcript).

## Odyssey checklist

- [ ] Treat **four-column project detail** as the **canonical** SysForge project UI when porting; do not port legacy scroll `ProjectDetailView` as primary.
- [ ] Update stale Odyssey research: `Sysforge research/features/projects-work-orders.md` still says sandbox four-column “may not match final production UI”; `Sysforge research/01-feature-inventory.md` still marks project detail as Partial/sandbox — both conflict with `d45fa40`.
- [ ] Port or re-specify columns/capabilities reflected by `ProjectDetailFourColumnView` / `ProjectDetailFourColumnViewModel` (project picker header, photo before/after, screw map preview, invoice return navigation via `InvoiceNavigationReturnContext`).
- [ ] Keep `NavigationService` behavior: requests for `ProjectDetailViewModel` redirect to `ProjectDetailFourColumnViewModel`.
- [ ] Confirm `MainWindowViewModel` title/mapping for `ProjectDetailFourColumnViewModel` (“Project”) if Odyssey has an equivalent shell.
- [ ] Designer/parameterless dialog ctor pattern is Avalonia-specific; skip or replace with Odyssey web equivalents when porting dialogs.
- [ ] Verify commit `d45fa40` / branch `cursor/projects-plan-7229` content is the source of truth for what landed (transcript redacted the file list).

## Confidence

**Medium-high on intent, medium on file-level detail.** Transcript is short and complete; no subagents. Commit message and post-push summary name the feature clearly. Full diff / 12 file paths were redacted, so exact dialogs and deleted files need a git look at `d45fa40` if Odyssey needs a precise port inventory.
