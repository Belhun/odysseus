# Title/topic

Commit uncommitted SysForge work: create-project-from-invoice, draft timezone fix, screw-map/product planning docs

- **Project:** SysForge
- **UUID:** `c7be02df-5f41-4347-9d79-38ffd95e4cd0`
- **Branch (at chat time):** `cursor/projects-plan-7229`
- **Sibling subagent transcripts:** none
- **Outcome:** three commits; working tree clean; not pushed in-chat

| Hash | Message |
|------|---------|
| `fbeb6d8` | `feat(projects): add create-project-from-invoice flow with picker dialogs` |
| `20ecd59` | `fix(drafts): use local timezone in default draft names` |
| `3947241` | `docs: screw map status, product questionnaire, and S5 mobile plan` |

---

## Features

### Create project from invoice (`fbeb6d8`)

Shared workflow to start a repair project from an estimate/invoice:

- **`CreateProjectFromInvoiceService`** — single pipeline: optional invoice pick → device pick → setup dialog → navigate to project detail
- **`SelectInvoiceDialog`** — recent invoices + search
- **`SelectInvoiceDeviceDialog`** — choose device when invoice has multiple devices without projects
- **`InvoiceService.GetRecentInvoicesAsync`** — backs the invoice picker
- **Invoice preview panel** — “Create project from invoice” button while previewing an invoice
- **`CreateProjectDialog`** — parameterless ctor for Avalonia XAML tooling
- Wired to existing entry points via DI: `ProjectDetailFourColumnViewModel`, `MainWindowViewModel` header action (`ServiceCollectionExtensions`)
- Also touched sandbox wireframe VMs: `ClientDashboardWireframeViewModel`, `ClientDashboardWireframeClassicViewModel`

### Planning / product docs (`3947241`) — product surface, not runtime code

- **`Product-Decisions-Questionnaire.md`** — master fill-in checklist (Parts I–X)
- **`Screw-Map-Decisions-Questionnaire.md`** — stub → Part IV of master questionnaire
- **`Screw-Map-S5-Mobile-Companion-Plan.md`** — S5 mobile photo upload options: LAN browser, inbox folder, relay tunnel
- Sync updates: Projects plan, Repair-Documentation-System, Database-Design-Document, Open-Questions, Plans README (S0–S4 screw map marked shipped)

---

## Issues/bugs

### Draft default name timezone (`20ecd59`)

- **Bug:** default draft names used UTC `CreatedAt`, so evening local sessions could show the next calendar day
- **Fix:** `DraftService` + `DraftWorkflowTests` route display/naming through `DateTimeHelpers.ToLocal`
- **Odyssey relevance:** any draft naming or “created” labels must use local time (or an explicit shop timezone), not raw UTC

No other product bugs were diagnosed in this chat; the session was investigate-and-commit, not debug.

---

## UX

- Invoice → project: multi-step picker flow (invoice when needed, then device for multi-device estimates, then setup)
- Invoice preview: direct “Create project from invoice” affordance (avoids hunting for project entry points)
- Draft names: date in the name should match the user’s local day (timezone fix above)

---

## Plans/deferred

From committed planning docs (not built in this chat):

- **Screw map S5 mobile companion** — still a plan; compare LAN browser vs inbox folder vs relay tunnel
- **Product-Decisions-Questionnaire** — open decisions across Parts I–X (fill-in, not shipped)
- Screw-map questionnaire stub redirects to Part IV of the master questionnaire
- Push of the three commits to `origin/cursor/projects-plan-7229` was offered but not done in-chat

---

## Odyssey checklist

Use these when verifying odysseus-sysforge parity or ports:

- [ ] **Create project from invoice** — shared service/route that can start from invoice id or open an invoice picker
- [ ] **Invoice picker** — recent list + search (equivalent of `GetRecentInvoicesAsync` + `SelectInvoiceDialog`)
- [ ] **Device picker** — when estimate/invoice has multiple devices without linked projects
- [ ] **Invoice preview CTA** — “Create project from invoice” on invoice preview/detail
- [ ] **Entry points** — header/project views can invoke the same shared flow (not duplicated logic)
- [ ] **Draft default names / dates** — format with local (or shop) timezone, not UTC-only
- [ ] **Draft timezone regression test** — evening local time must not roll the displayed date forward
- [ ] **Screw map S0–S4** — treat as shipped in SysForge plans; confirm Odyssey inventory matches
- [ ] **S5 mobile companion** — still deferred; do not assume LAN/inbox/relay is implemented
- [ ] **Product decisions questionnaire** — track open Parts I–X when scoping Odyssey feature cuts

---

## Confidence

**Medium–high** for what this chat covered.

- Full parent transcript read (~11 turns); no sibling subagent jsonl
- Features, bug, and docs are stated clearly in the agent’s commit summary and file list
- Chat does not include deep UX walkthroughs, edge-case testing notes, or Odyssey-specific design; checklist items are inferred from SysForge commit intent
- Commit hashes and branch name are from the chat transcript only; not re-verified against current SysForge git state
