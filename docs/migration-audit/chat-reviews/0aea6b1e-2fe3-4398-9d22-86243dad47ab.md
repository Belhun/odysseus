# Chat review: 0aea6b1e-2fe3-4398-9d22-86243dad47ab

## Title/topic

BUG-015 unit test suite repair: replace drifted / falsely failing tests with behavior-real assertions (JSON helpers, InvoiceItems FK, draft retention, draft naming, DraftData↔Invoice schema sync).

- Project: SysForge (Avalonia desktop)
- Transcript: `...\agent-transcripts\0aea6b1e-2fe3-4398-9d22-86243dad47ab\0aea6b1e-2fe3-4398-9d22-86243dad47ab.jsonl`
- Sibling subagent `*.jsonl`: none
- Branch / commit: `async-db-migration` → `bf073a2` (`test: fix BUG-015 drifted tests with real behavior assertions`), pushed to `origin/async-db-migration` (https://github.com/Belhun/SysForge.git)
- Product app code changed: **none** (tests + plan docs only)

## Features

No new product features. Chat locks **contracts** that the port must preserve:

| Area | Concrete names | Contract from chat |
|------|----------------|--------------------|
| JSON field helpers | `Part.GetCompatibleDevices`, `Part.GetTags`, `Client.GetAssociates`, `Supplier.GetContactInfo`, `Supplier.GetShippingInfo` | Invalid JSON → return `null`, do not throw; raw DB string left for repair; valid JSON parses to `List<string>` / `Dictionary<string, object>` |
| Invoice line items | `Invoice` (no `Items` prop), `InvoiceService.GetInvoiceItemsAsync`, `InvoiceItems` table, `DraftData.Items` / `DraftItem` | Persisted lines live in DB via service; drafts keep in-memory `List<DraftItem>` for calculator state |
| Money shape | `DraftData.TaxRate`, `PartsSubtotal` vs `Invoice.TaxRateBasisPoints`, `PartsSubtotalCents`; `DraftItem.DiscountValue` (`decimal`) vs `InvoiceItem.DiscountValue` (`long`); migration `0012_money_to_cents.sql` | Draft UI decimals; invoice DB cents / milliunits / basis points |
| InvoiceItems schema | `QuantityMilliunits` (not `Quantity` after `0012`) | FK tests and inserts must use post-0012 columns |
| Draft retention | `DraftService.DeleteOldDraftsAsync`, `Config.Drafts.AutoDeleteOld`, `RetentionMonths`, `LastModifiedAt`, helper `PersistDraftLastModifiedAsync` | `SaveDraftAsync` always rewrites `LastModifiedAt` to `UtcNow`; retention ages must be written to on-disk JSON after save |
| Draft naming | `NormalizeDraftName` / save path vs `DraftData.DisplayName` | Save with null name → `"Unknown Client - MM/dd/yyyy"`; empty name+client `DisplayName` → `"Untitled Draft"` + date/time |
| FK enforcement | `MigrationsTests.Foreign_keys_enforced_on_invoice_items`, `PRAGMA foreign_keys`, orphan `InvoiceId` | Reject orphan inserts; Microsoft.Data.Sqlite: primary `SqliteErrorCode == 19`, extended `SqliteExtendedErrorCode == 787` |

Shared scalar fields asserted between `DraftData` and `Invoice`: `ClientId`, `IncludeTax`, `IncludeShipping`. Shared line fields on `DraftItem`/`InvoiceItem`: `PartId`, `PartName`, `SKU`, `Quantity`, `UnitPrice`, `DiscountType`, `DiscountValue`, `IsTaxable`, `ItemType`, `SortOrder`, computed `LineTotal` (`JsonIgnore` on at least one).

Prior context from same “what’s next” turn (not implemented here): **BUG-018** (JSON import → client search) already committed on `async-db-migration`.

## Issues/bugs

| ID / name | Status in chat | Detail |
|-----------|----------------|--------|
| **BUG-015** | **Fixed** (2026-05-20): `dotnet test` **94 passed, 0 failed**; closed in `Plans/Open-Bugs-And-Investigations.md` | Root causes were bad tests, not app regressions: wrong reflection (`ReturnType.GetGenericArguments()[0]` instead of `ReturnType`), INSERT used removed `Quantity` column (failure was “no such column,” not FK), FK asserted `SqliteErrorCode == 787` instead of extended code, `DeleteOldDrafts` set age then save overwrote to now, naming expected `Untitled Draft` on save path |
| **BUG-010** | Still open / optional | Remove `AppendAllText` debug logging in `InvoiceCalculatorView.axaml.cs` and `InvoiceCalculatorViewModel.cs` |
| **BUG-017** | Referenced as deferred product work | Auto-delete drafts; tests kept behavior coverage rather than skipping once retention date persistence was fixed |
| Fake / weak tests (user ask) | Addressed | User required tests that fail when behavior breaks; chat rewrote assertions to hit real paths |

Files touched: `SysForge.Tests/Database/JsonSchemaSyncTests.cs`, `MigrationsTests.cs`, `DraftServiceTests.cs`, `DraftWorkflowTests.cs`, `SchemaSyncTests.cs`; plans `Open-Bugs-And-Investigations.md`, `README.md`.

## UX

- Minimal UX in this chat. Naming strings users see:
  - Saved draft with no name: **Unknown Client** + date (`MM/dd/yyyy`).
  - Display label with no name/client: **Untitled Draft** + date/time.
- Invalid JSON in part/client/supplier JSON columns must not crash UI; return null and leave raw string for repair flows.
- Manual smoke on backup/import after BUG-018 still recommended in the “what’s next” advice (not executed here).

## Plans/deferred

| Item | Source | Notes |
|------|--------|--------|
| **BUG-010** debug cleanup | Plans top priority after BUG-015 | Calculator `AppendAllText` instrumentation |
| **APP-07** async DB migration | `Post-Implementation-Improvements.md`; branch `async-db-migration` | Docs still said “Planned” at chat start; finish migration or update plan status |
| Deferred backup/restore (Improvement 5) | Plans | Import conflict UI, SQL import button, custom backup folder |
| **Future UI** | `Plans/UI-Plans/Future-UI-Features.md` | Forward backlog |
| **BUG-017** draft auto-delete product wiring | Linked to retention tests | Service exists; startup/auto wiring may still be incomplete (also noted in odysseus research `draft-management.md`) |

## Odyssey checklist

Port / verify in `F:\Codeing Project\odysseus-sysforge` (SysForge add-on + research):

- [ ] **JSON helper semantics** — Parts/clients/suppliers JSON columns: bad JSON → `null` / no throw; raw string retained; cover in Python tests analogous to `JsonSchemaSyncTests`.
- [ ] **Invoice vs draft item model** — No embedded `items` on invoice ORM entity; load via invoice-items API/service; drafts keep items list for calculator (`Sysforge research/features/draft-management.md`, invoice calculator research).
- [ ] **Money units** — Port `0012` conventions: cents / `QuantityMilliunits` / tax basis points in DB; draft/calculator may stay decimal until save (`Sysforge research/04-data-model-migration.md`, `00-overview.md`).
- [ ] **InvoiceItems FK** — Enforce FK with `PRAGMA foreign_keys=ON` (already in `core/database.py` / `tests/test_sqlite_foreign_keys.py`); orphan `invoice_id` insert must fail on real schema columns.
- [ ] **Draft retention** — If porting `DeleteOldDrafts` / `AutoDeleteOld` / `RetentionMonths`: do not trust in-memory timestamps after save; age is file JSON `lastModifiedAt`. Wire startup/cron (research: method exists, not called from SysForge `Program.cs`).
- [ ] **Draft naming contracts** — Split save-normalization (`Unknown Client`) from display fallback (`Untitled Draft`); add regression tests.
- [ ] **Schema-sync style tests** — Keep drift guards between draft and invoice models when either side changes (shared fields + intentional money-type differences).
- [ ] **Avoid fake tests** — Prefer behavior assertions over brittle reflection; if reflecting, assert the full return type, not generic type args that always “pass” the wrong check.
- [ ] **BUG-010** — Confirm calculator debug file logging is absent in Odysseus JS/Python ports of invoice calculator.
- [ ] **Cross-check BUG-018** — Import→search reindex still required for client search parity (see chat `11201750-…`); this chat only notes it as already committed on desktop branch.

## Confidence

**High** for BUG-015 root causes, fixed contracts, file list, and commit `bf073a2` on `async-db-migration`: full parent transcript read (33 lines); no sibling subagents; suite reported green in-session.

**Medium** for deferred backlog items (APP-07 status, BUG-017 wiring, backup Improvement 5): mentioned as next steps, not verified in this chat.

**Low relevance** for direct Odysseus UI port from this chat alone: no product/UI changes; value is behavioral contracts and test-quality lessons for the add-on.
