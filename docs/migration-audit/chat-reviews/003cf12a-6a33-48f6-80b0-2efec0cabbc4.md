# Chat review: 003cf12a-6a33-48f6-80b0-2efec0cabbc4

**Source:** SysForge agent transcript (planning only; no product code changed in-chat)  
**Sibling subagents:** none (folder has only the parent `.jsonl`)  
**Date context:** synthesis of existing SysForge plans/docs vs then-current repo

---

## Title/topic

**Projects as the client work-unit hub** — what “Projects” means in SysForge product vision, how docs evolved (Jobs vs Projects naming), and a recommended **Projects v1** slice to start implementing after Phase 9 (clients/invoices).

---

## Features

Discussed / designed (not implemented in this chat):

- **Project** = unit of work for a client (repair job, IT engagement, remediation). Hub linking:
  - `ClientId`, title, status, schedule fields (later), invoices, repair specifics, parts, IT/asset links (later)
- **Early UI sketch** (`Plans/Archive/Early-Drafts/dashboard.pseudocode`): client search → panel with **Start New Project** → project list (ID, status, summary, date) → project details
- **v2 roadmap stubs** (`Plans/Product-Vision/Invoice-Roadmap.md`): `Jobs` table, fullscreen client tabs include Jobs, `LinkInvoiceToJob`
- **Repair docs dependency** (`Plans/Product-Vision/Repair-Documentation-System.md`): screw maps need `ProjectId`; HW projects get “Start Screw Map”
- **Plan B model** (`Plans/Product-Vision/External-Plans/Plan-B-Raw.md` / `Plan-B-Normalized.md`): `jobs` + `parts` + status machine + calendar webhook
- **Recommended v1 slice:**
  - Migration `0016_projects.sql` (or `jobs`): `Id`, `ClientId`, `Title`, `Status`, `Summary`, `CreatedAt`, `ClosedAt`, `Notes`
  - Optional same migration: `Invoices.ProjectId` FK
  - `Models/Project.cs`, `Database/ProjectService.cs` (`AddProject`, `UpdateProject`, `GetProjectsByClient`, `GetProjectById`, `LinkInvoiceToProject`)
  - DI in `ServiceCollectionExtensions.cs`; CRUD tests in `SysForge.Tests`
  - Wire `ClientDashboardView` / `ClientDashboardViewModel`: list on `SelectedClient`, create (Title + Status default Active), open detail, link invoice
  - Thin `ProjectDetailView`: title/status/notes, client link, linked invoices, **New invoice for this project**, Back
  - `InvoiceCalculatorViewModel` optional `ProjectId` preset; persist on save; optional read-only “Current projects” side list
  - Document as **Phase 10** (`Plans/Implementation-Plan/Phase-10/README.md`)

**Then-current repo state (per chat):** no `Jobs`/`Projects` migration (latest cited `0015_invoices_last_edited.sql`); no `JobService` / project detail view; Client Dashboard invoice-centric; sandbox placeholders only (“Projects — coming later”).

**Later SysForge reality (outside this chat; for Odyssey cross-check):** research notes claim `0016_work_orders.sql`, `0017_invoice_devices.sql`, `0018_projects.sql`, `ProjectService`, `ProjectsHubViewModel` / `ProjectsHubView`, `CreateProjectFromInvoiceService`, work orders — treat chat’s “not started” as historical.

---

## Issues/bugs

- **Naming drift:** UI/language = **Projects**; v2 schema/DAL language = **Jobs** (`ProjectId` in screw-map tables). Unresolved in-chat; risk of fighting the codebase.
- **Repair documentation blocked** on “project system foundation” (explicit dependency order in `Repair-Documentation-System.md`).
- **Implementation priority gap:** `Implementation-Priority-Order.md` ends at Phase 9; projects not scheduled as Phase 10 yet (at chat time).
- No runtime bugs reported; this session was research/planning only.

---

## UX

- Client Dashboard: list projects when client selected; **Start New Project** (dialog or inline); open project → `ProjectDetailView`; link invoice from invoice row or project detail.
- Layout choice deferred: sandbox **Classic** four-column vs **Preview+** center Projects tab (`Client-Dashboard-Prototypes.md` placeholders).
- Project detail (v1): editable title/status/notes; read-only client; linked invoices list; **New invoice for this project** (calculator with client + `ProjectId`); Back. No screw-map button until `Type = HW`.
- Calculator: optional `ProjectId` when opened from project; small current-projects list when client selected (pass 1).
- Long-term lanes sharing one project concept: **repair workflow** vs **managed IT** (`Source-Notes.md`, `Use-Cases.md`, `Data-Sync-Model.md`).

---

## Plans/deferred

Out of scope for v1 slice (ordered after v1 in chat):

1. Status workflow + dates (Plan B; n8n calendar)
2. `ProjectType` (HW / IT / Maintenance) — unlocks screw-map rules
3. Parts on project (`ProjectParts`)
4. Screw maps (`Repair-Documentation-System.md`)
5. Fullscreen client (Profile / Invoices / Jobs / Notes)
6. Wazuh / Snipe-IT / assets (`Requirements.md`, Plan B)

Also deferred from minimal schema: `Type`, `PlannedStart`, `ActualStart`, `ActualEnd`, `DeviceModel`, `AssetId`.

Open decisions before coding (asked of user; no answer in transcript):

1. Projects vs Jobs (UI + table name)
2. Status enum: simple (Active/Complete/Closed) vs Plan B (Planned/WaitingParts/InProgress/…)
3. Human-readable project codes (e.g. `250216-IP14-HW-001`) vs integer `Id` only for v1
4. Classic vs Preview+ dashboard layout for projects panel

---

## Odyssey checklist

Port/check in `F:\Codeing Project\odysseus-sysforge` (SysForge plugin / Business Management):

- [ ] Confirm product term: UI **Project**, table/API `projects` (align with `docs/research/sysforge/features/projects-work-orders.md` and plugin copy in `integrations/sysforge/manifest.json`)
- [ ] Port Projects schema before UI (`0018_projects.sql` / related `0016`–`0018` work-order + invoice-device migrations — do not rely on this chat’s suggested lone `0016_projects.sql` shape without reconciling desktop truth)
- [ ] API surface: list by client, get by id, create/update, link invoice (`Invoices.ProjectId` or equivalent), create-from-invoice if desktop has `CreateProjectFromInvoiceService`
- [ ] Map chat’s `ProjectService` CRUD to `services/sysforge/projects.py` (see `docs/research/sysforge/05-backend-services.md`: `GET/POST /projects`, `GET /projects/{id}`, `POST /projects/from-invoice`)
- [ ] Client dashboard / hub: list + create + navigate to detail (`projects-hub.js` / `ProjectsHubViewModel` parity)
- [ ] Thin project detail: status/notes, linked invoices, “new invoice for this project” with `project_id` preset on calculator
- [ ] Invoice calculator: accept/persist optional `project_id`
- [ ] Keep screw maps, parts-on-project, Wazuh/Snipe-IT, n8n calendar **out of MVP**; screw maps still need stable `ProjectId` (`docs/research/sysforge/features/screw-maps.md`)
- [ ] Status enum and human-readable project codes: resolve against `Product-Decisions-Questionnaire.md` / desktop CHECK constraints before locking API
- [ ] Plugin shell already mentions projects as future module (`integrations/sysforge/static/js/index.js`); replace placeholder when module lands

---

## Confidence

**Medium–high on intent and v1 scope** (full 10-line transcript read; clear synthesis from named SysForge plans).  
**Medium on “current desktop state”** — chat asserted no Projects migration yet; later Odyssey research documents schema/hub as in progress; verify against live SysForge migrations/services before porting.  
**Low on UX layout decision** — Classic vs Preview+ never chosen in-chat.
)
