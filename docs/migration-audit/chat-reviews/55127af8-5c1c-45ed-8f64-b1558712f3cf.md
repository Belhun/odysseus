# Chat review: 55127af8-5c1c-45ed-8f64-b1558712f3cf

**Source:** SysForge agent transcript  
**Transcript:** `C:\Users\User\.cursor\projects\f-Codeing-Project-SysForge\agent-transcripts\55127af8-5c1c-45ed-8f64-b1558712f3cf\55127af8-5c1c-45ed-8f64-b1558712f3cf.jsonl`  
**Siblings (9):** `2a012dcb` (DB-12), `a371e8fd` (Q-05), `9267b72c` (README), `be40d559` (cross-audit), `fa5dd674` (export DB→JSON), `d85fec42` (sample JSON), `9c8d75a0` (commit/push), `2fcd0163` (plan status), `e00cefa8` (BUG-018)  
**Cite:** [Post-impl README, DB-12, Q-05](55127af8-5c1c-45ed-8f64-b1558712f3cf)  
**Related reviews:** `c2396042` (BUG-018 harden), `a162de19` (deferred backlog), `8eceb374` (tests)

## Title/topic

Ship three post-implementation items in one session: **DOC-01/DOC-02** root README, **DB-12** duplicate warnings on Invoice Calculator, **Q-05** backup/restore (+ export arbitrary `.db` to JSON, sample import fixture). Follow-on: audit/fix, plan status, push (`aa2ee75` on `async-db-migration`), then **BUG-018** (JSON import skips Lucene reindex).

## Features

### DOC-01 / DOC-02 — Root README
- New root `README.md`: project description, quick start (build/run/test, `%LOCALAPPDATA%\SysForge\sysforge.db`), architecture (Avalonia/SQLite/DI/MVVM), categorized documentation index, migration/transaction notes, project structure.
- Links to `Plans/README.md` as planning entry point; 23 paths verified at authorship (audit later fixed one broken link).

### DB-12 — Enhanced duplicate detection (Invoice Calculator only)
- `ClientService`: `FindClientsByPhoneAsync`, `FindClientsByEmailAsync`, `FindPotentialDuplicatesAsync`; stronger DB fallback (normalized phone, nickname).
- `ClientQueryParser` (new): classifies query as phone / email / name.
- `ClientSearchResult.SecondaryText`: phone + email in dropdown.
- `InvoiceCalculatorViewModel` + `InvoiceCalculatorView.axaml`: real-time yellow banner on phone/email-shaped input; create-time check with **Use this client** / **Create new client anyway** (shared contact allowed; no DB uniqueness).
- Tests: `SysForge.Tests/Database/ClientDuplicateDetectionTests.cs` (8 tests).
- Comment/stub points to future `ClientMergeView` (not built).
- Scope assumption: **Client Dashboard not changed**.

### Q-05 — User backup and restore
- `BackupModels.cs`: `SysForgeExportData` v1.0, `ImportOptions`, DTOs.
- `BackupService.cs`: manual `.db` backup + config sidecar; restore with integrity check + safety backup + `SqliteConnection.ClearAllPools`; JSON export/import (clients, invoices, items, parts, settings); SQL dump export; `ImportFromSqlDump`; retention cleanup; WAL checkpoint.
- `ScheduledBackupService.cs` + `BackupConfig` in `ConfigService` / `config.json`; started from `Program.cs`.
- Settings → **Backup and restore**: Create backup, Restore, Export JSON, Export SQL, Import JSON, schedule enable/interval/retention, optional include drafts.
- Later: **Export database file to JSON…** → `ExportDatabaseToJson(databasePath, exportPath)` (read-only source `.db`/`.sqlite`; sibling `*-config.json` if present).
- Tests: `BackupServiceTests` grew through session (~7 → 13 by BUG-018 fix).

### Sample test data
- `SysForge.Tests/TestData/sample-export.json`: 12 clients, 10 invoices, 19 line items, 10 parts; Walsh shared phone `(555) 234-5678`; Jenkins shared email `family@jenkins.com`; statuses Estimate/Invoiced/Paid.
- Import path: Settings → **Import from JSON**.
- Import fix: write `Status` / `DiscountType` / `ItemType` as text (CHECK constraints).

### Ship
- Commit `aa2ee75` on `async-db-migration` → `origin` (`feat: README, duplicate detection, backup/restore, and tests`). BUG-018 fix was **local/uncommitted** at chat end.

## Issues/bugs

| ID / topic | Detail | Resolution in this chat |
|------------|--------|-------------------------|
| **BUG-018** | JSON import shows in Diagnostics (`GetAllClientsAsync`) but Client Dashboard / Invoice Calculator search miss clients | Root cause: `ImportFromJson` bypasses `ClientService` indexing. Fix: inject `SearchService` into `BackupService`; `RebuildSearchIndexAsync` after import when clients imported. Note in `Plans/Open-Bugs-And-Investigations.md` + Q-05 follow-up. **UI verify still open** at chat end; later chat `c2396042` hardens Skip/stale-index cases. |
| Lucene phone/email gap | With Lucene on, bare phone/email ignored `phone_norm` | Audit merged `FindClientsByPhoneAsync` / `FindClientsByEmailAsync` into `SearchClientsAsync` |
| README link | `Database%20Plan.md` 404 on disk | Point to `Database Plan.md` |
| SQL dump restore | Whole-script `ExecuteNonQuery`; delete live DB under lock | `ExecuteSqlScript` per statement; `ClearAllPools` + file copy |
| JSON enum CHECK | Import wrote enums wrong for SQLite CHECKs | Text mapping aligned with `InvoiceService` |
| Full suite | ~9 pre-existing failures (BUG-015 schema/draft drift) | Left alone; filter `BackupServiceTests` / `ClientDuplicateDetection` |

## UX

- **Duplicate banner (Invoice Calculator):** yellow, informational while typing phone/email; on create, pause with match list + **Use this client** / **Create anyway**.
- **Settings → Backup and restore:** expander; toasts for success/error; restore toast says **restart** (no auto-restart).
- **Export database file to JSON…:** pick source DB → pick save path (`{dbname}-export.json` default).
- **Import conflict:** Skip only in Settings UI at ship time (Overwrite mentioned for sample IDs 900xx later).
- Manual smoke list from parent: import `sample-export.json` → search Maria/Chen/`(555) 123-4567` in Dashboard + Calculator; Walsh/Jenkins duplicate pairs; backup → restore → restart.

## Plans/deferred

- **Client merge tool** (`ClientMergeView`) — DB-12 Part 3; explicitly deferred.
- **Import conflict UI** (overwrite/merge/selective tables) — service may support more; Settings uses Skip.
- **Import from SQL dump** Settings button — `ImportFromSqlDump` exists; no button.
- **Auto-restart after restore.**
- **Custom backup folder picker** for manual backups (default `%LOCALAPPDATA%\SysForge\backups\` only).
- **`Plans/Database-Design-Document.md`** backup section — skipped.
- **Improvement 1 (APP-07 async DB)** — still planned.
- Plan docs marked Improvements 2/4/5 **Done (2026-05-18)** with deferred checkboxes left open (`Post-Implementation-Improvements.md`, `Plans/README.md`, verification notes).
- BUG-018 fix **not in `aa2ee75`**; parent asked to verify with uncommitted build.

## Odyssey checklist

Checked against `F:\Codeing Project\odysseus-sysforge` at review time (host has `routes/backup_routes.py` / `docs/backup-restore.md` for Odysseus data; `integrations/sysforge` only `mkdir backups` — no shop-domain backup/duplicate UI).

- [ ] Port **DB-12** into web invoice/client create: warnings not hard blocks; phone/email real-time match; **Use existing** / **Create anyway**; no unique constraint on shared contact.
- [ ] Port `FindClientsByPhone` / `FindClientsByEmail` / `FindPotentialDuplicates` + query classification (`ClientQueryParser` behavior) for search when full-text/Lucene equivalent is on.
- [ ] Keep **merge tool** out of v1; document as follow-up (`ClientMergeView` analog).
- [ ] Port **Q-05** shop backup: portable `SysForgeExportData`-style JSON (clients, invoices, items, parts, settings); manual backup/restore; scheduled backups + retention if product wants parity.
- [ ] Align formats with Odysseus ZIP backup (`docs/backup-restore.md`) vs SysForge JSON — decide one interchange story for the plugin.
- [ ] Settings UI: Create backup, Restore, Export JSON, Export SQL, Import JSON, **Export database file to JSON** (pick arbitrary `.db`).
- [ ] **BUG-018 rule:** any bulk import that bypasses normal client CRUD **must rebuild or update search index**; Diagnostics ≠ search. Cover Skip re-import / stale index (see `c2396042`).
- [ ] `.db` restore must not leave stale search index (called out as related gap).
- [ ] Sample fixture pattern: multi-client JSON with shared phone/email pairs for QA (Walsh/Jenkins style).
- [ ] Import enums/statuses as DB-safe text (CHECK / constraint parity).
- [ ] Defer: SQL import button, conflict UI, auto-restart, custom backup folder — match desktop deferred list unless Odyssey needs them earlier.
- [ ] README/docs for contributors: Odyssey/plugin entry docs are separate from desktop `README.md`; only port content that applies to the web stack.
- [ ] Cross-check research/reviews: `a162de19` checklist, `c2396042` BUG-018, `docs/research/sysforge` post-impl items when present.

## Confidence

**High** for scope and concrete artifacts (9 subagents with deliverable summaries; file lists; test counts; BUG-018 root cause).  
**Medium** for final desktop commit state of BUG-018 (uncommitted here; later chat claims further harden + ship).  
**High** that Odyssey still needs shop-domain backup + duplicate-detection ports; host backup alone is not enough.
