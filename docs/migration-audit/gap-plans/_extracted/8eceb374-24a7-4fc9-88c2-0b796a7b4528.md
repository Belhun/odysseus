# Chat review: 8eceb374-24a7-4fc9-88c2-0b796a7b4528

**Source:** SysForge agent transcript (~120KB, 72 JSONL lines)  
**Sibling subagents:** none (folder has only the parent `.jsonl`)  
**Product code changed:** **tests only** in SysForge (no app/production changes expected)  
**Git outcome:** pushed commit `9288109` on `origin/async-db-migration` — `test: expand SysForge.Tests with solid regression coverage` (+1,033 lines, 8 files)  
**odysseus-sysforge mentioned in chat:** no (0 hits); Odyssey notes below are migration inferences

---

## Title/topic

**SysForge.Tests expansion (solid tests only)** — clarify Tests vs Sandbox, plan high-value regression/security-adjacent coverage, trim flaky items, implement ~37 new tests (94 → **131** passing), explain xUnit1031 warning, push to git.

Plan file (not edited during implement phase): `C:\Users\User\.cursor\plans\sysforge.tests_expansion_de65f605.plan.md`.

---

## Features

### Clarified project roles (not new product features)

| Project | Role |
|---------|------|
| **`SysForge.Tests`** | Real regression/integration suite; references `SysForge.csproj`; xUnit against `DatabaseService`, `MigrationRunner`, `DraftService`, `BackupService`, etc.; CI via `.github/workflows/dotnet.yml` |
| **`SysForge.Sandbox`** | Idea/assumption sandbox; no app project reference; topics like `DateTime/`, `Money/`, `Database/`; **not** in GitHub Actions |

### Test infrastructure added

- **`SysForge.Tests/TestInfrastructure/FileBackedTestContext.cs`** — temp file DB under `%TEMP%/SysForge.Tests/`, one-time Dapper enum handlers (`InvoiceStatus`, `DiscountType`, `ItemType`), `MigrationRunner.RunMigrations()`, dispose cleans `.db`/`-wal`/`-shm` and `InvoiceBackups/`
- Existing **`DbTestFixture`** kept for in-memory migration tests

### Phase 1 — unit helpers

- **`MoneyHelpersTests.cs`** (8) — `ToCents`/`FromCents`, AwayFromZero rounding (`10.995m` → `1100`), basis points, milliunits, `CalculateTaxCents`, `CalculateLineTotalCents`
- **`InvoiceValidationHelpersTests.cs`** (4) — `ValidationException` for negative `FinalTotal`, tax > 100%, empty `PartName`, quantity ≤ 0

### Phase 2 — service tests (file-backed DB)

- **`InvoiceServiceTests.cs`** (7) — new save + items/`*Cents`; **BUG-006/007** update-same-id (no second invoice); replace items; invalid item `ThrowsBeforePersist`; status as text; delete header+items; `GetInvoicesByClientAsync` filter
- **`ClientServiceTests.cs`** (6, DB only) — add/update; soft delete sets `IsDeleted`; `GetClientByIdAsync` null after soft delete; validation negatives (no identifier / invalid email) leave counts unchanged
- **`PartServiceTests.cs`** (6) — CRUD; unused delete; usage count; placeholders; referenced delete behavior (see Issues)

### Phase 3 — backup + migrations

- Extended **`BackupServiceTests.cs`** — invalid JSON; FK mid-import rollback; `ConflictResolution.Skip` / `Overwrite`; stronger assert on `RestoreBackup_RejectsInvalidDatabase` (live row counts)
- **`MigrationRunnerTests.cs`** — tamper `SchemaVersion.Checksum` for Id **2** (`0002_settings.sql`); expect `MigrationChecksumMismatchException`; idempotent second `RunMigrations()`

### Pre-existing coverage called out as already valuable

Backup/import/restore (incl. BUG-018 Lucene reindex), drafts, placeholders/invoices, schema/JSON sync, FK/triggers, client duplicate detection / query parser.

---

## Issues/bugs

| Name | Status in chat | Detail |
|------|----------------|--------|
| **BUG-015** | Historical baseline | Plans noted ~10 failing tests as of 2026-05-17; by implement time suite was **94 passing**, then **131** green |
| **BUG-006 / BUG-007** | Covered by new test | Invoice edit must update same id, not create a second invoice (`SaveInvoiceAsync_UpdateSameId_DoesNotCreateSecondInvoice`) |
| **BUG-018** | Already covered; not re-done in ClientService | `ImportFromJson_RebuildsSearchIndexSoImportedClientsAreSearchable` in `BackupServiceTests`; Lucene client search kept out of new `ClientServiceTests` to avoid CI lock flakes |
| **Plan wrong: soft-delete vs `GetAllClientsAsync`** | Corrected in plan review | `GetAllClientsAsync` **includes** soft-deleted (diagnostics-style); exclusion is via `GetClientByIdAsync` / search index |
| **Plan wrong: migration Id 1** | Corrected | First migration file is **`0002_settings.sql`**; checksum tamper uses `WHERE Id = 2` |
| **Plan wrong: invoice “rollback” wording** | Corrected | `InvoiceValidationHelpers.Validate*` runs **before** `BeginTransaction`; use `ThrowsBeforePersist` |
| **`DeletePartAsync` + FK** | Test adjusted to real schema | Expected FK throw was wrong; schema uses **`ON DELETE SET NULL`** on `InvoiceItems.PartId` → test renamed `DeletePartAsync_WhenReferenced_RemovesPartAndNullsLineItemPartId` |
| **Settings tests risk** | Deferred (not implemented) | `ConfigService` writes real `%LOCALAPPDATA%/SysForge/config.json` |
| **SQL dump = arbitrary SQL** | Known product gap; test deferred | `ImportFromSqlDump` / `ExecuteSqlScript` can run destructive SQL; chat warned not to add “DocumentsCurrentBehavior_” tests without a product fix |
| **xUnit1031** | Pre-existing warning; not fixed | Sync `[Fact]` blocks on `InitializeAsync().GetAwaiter().GetResult()` in BUG-018 test (~line 460); recommend `async Task` + `await`; also in `CreateSearchBackedServices()` ~399 |
| **Dapper enum handlers** | Mitigated in fixture | Must register once; repeated `AddTypeHandler` throws |

---

## UX

This chat is almost entirely **test/CI** work; little direct UI change.

Migration-relevant UX/behavior still encoded in tests:

- Soft-deleted clients stay visible in diagnostics-style **`GetAllClientsAsync`**, but disappear from **by-id** and **search** paths after index update.
- Import conflict UX modes **`Skip`** / **`Overwrite`** (clients/parts) are regression-tested; **`Merge`** is null-fields-only and was left out of solid tests (invoices have no Merge).
- Failed restore must leave the live DB unchanged (row-count assert).
- Money UX depends on cents/basis-points/milliunits helpers; wrong rounding shows up as billing errors.

---

## Plans/deferred

From trimmed plan (“iffy” removed) and review notes:

| Deferred item | Why |
|---------------|-----|
| **`SqliteFileBehaviorTests`** (WAL/`-shm`, busy timeout) | Windows CI timing flakes |
| **`DraftServiceSecurityTests`** (path traversal `../`, corrupt JSON) | OS-dependent; may need `DraftService` ID guard as product change |
| **`QueryBuilderTests`** (Lucene operators `+`, `-`, `&&`, `field:`) | Unclear pass/fail; crash-avoidance only |
| **`SettingsServiceTests`** | Mutates real user `config.json` unless fixture backup/restore or test-only path |
| **SQL dump allowlist / destructive import tests** | Documents unsafe behavior; needs product change first |
| **Import `Merge` / wrong JSON types** | Brittle merge semantics; deserializer variance |
| **`SearchClientsAsync` in `ClientServiceTests`** | Lucene parallel/index lock risk |
| **UI / ViewModel / `CurrentView` lifecycle tests** | Low ROI vs manual; double-subscribe already a known rule |
| **`ScheduledBackupService` timing tests** | Low ROI unless backup bugs recur |
| **Refactor `PlaceholderWorkflowTests` onto `FileBackedTestContext`** | Optional cleanup |
| **xUnit1031 async refactor** of BUG-018 test | User declined (“no your good”); left as warning |

Also noted earlier (pre-trim) as valuable but not shipped here: broader `FinalizeInvoiceAsync` / enum CHECK failures, import mid-loop atomicity variants beyond FK JSON, migration “applies pending in order” (called redundant with idempotent re-run).

Original “3 PRs” breakdown collapsed into one push on `async-db-migration`.

---

## Odyssey checklist

Port / verify in `F:\Codeing Project\odysseus-sysforge` (SysForge add-on + research). Chat never inspected Odyssey; items are cross-walked from implemented SysForge tests and research docs.

- [ ] **Port `MoneyHelpers` tests** — Research already flags this (`Sysforge research/04-data-model-migration.md`: “Port `MoneyHelpers` unit tests”). Cover cents round-trip, AwayFromZero (`10.995` → 1100), basis points, milliunits, tax/line totals. No equivalent under `tests/` for SysForge money yet.
- [ ] **Port `InvoiceValidationHelpers` negatives** — Reject bad totals/tax/empty part name/qty ≤ 0 before persist; mirror `ThrowsBeforePersist` (no fake “rollback” wording).
- [ ] **Invoice save/update identity (BUG-006/007)** — Update same invoice id; replace line items; delete removes header+items; status stored as stable string/enum.
- [ ] **Client CRUD + soft delete semantics** — Persist/reload; `IsDeleted`; by-id returns null after soft delete; list/diagnostics may still include deleted; search must exclude.
- [ ] **Part delete FK policy** — Do **not** assume FK reject; desktop uses **`ON DELETE SET NULL`** on line-item `PartId`. Match schema in add-on migrations or document intentional change.
- [ ] **Migration checksum enforcement** — Port `MigrationRunner` + `SchemaVersion` checksum mismatch (`MigrationChecksumMismatchException` parity). First desktop migration id is **`0002_settings`**, not `0001`. See `Sysforge research/features/database-infrastructure.md` / gap matrix (migrations still stub on install).
- [ ] **Idempotent migrate** — Second run must not duplicate `SchemaVersion` / applied set.
- [ ] **JSON import guards** — Invalid JSON fails safely; FK/orphan import rolls back (counts unchanged); conflict **Skip** vs **Overwrite**; no invoice Merge unless product adds it.
- [ ] **Failed restore leaves live DB intact** — Align with Odysseus `docs/backup-restore.md` / SysForge backup routes when SysForge domain backup lands (existing `tests/test_backup_*` cover host backup skills/memories, not shop invoices/clients).
- [ ] **File-backed / temp DB fixture pattern** — Prefer temp dirs with cleanup (WAL/shm + any invoice-backup side folders) over mutating real `%LOCALAPPDATA%` or user config.
- [ ] **Do not port SettingsService tests that call `Save()`** until config path is injectable.
- [ ] **SQL dump import** — If ported, treat allowlist/hardening as a product requirement; do not ship “arbitrary ExecuteSqlScript” without a security story.
- [ ] **BUG-018 search reindex after import** — Keep as separate checklist (already in other chat reviews); Lucene → FTS5/web search equivalent after bulk import.
- [ ] **Confirm commit `9288109` on SysForge `async-db-migration`** is merged/available before treating desktop test suite as source of truth for ports.
- [ ] **Plugin install tests** — `tests/test_sysforge_plugin.py` only covers install marker/catalog/404 when inactive; does **not** replace domain service tests above.

---

## Confidence

**High for what this chat did; medium for Odyssey gap status.**

- Transcript is complete, linear, and ends with a verified green run (131) plus a concrete push (`9288109`). No sibling subagents; tool noise is mostly explore/edit/test.
- Implemented file list and deferred list match the trimmed plan + final assistant summary; one schema correction (`ON DELETE SET NULL`) is explicit.
- Odyssey checklist is inferred (chat never opened `odysseus-sysforge`). Cross-check against current add-on code and `docs/migration-audit/synthesis/feature-gap-matrix.md` before treating boxes as current truth.
- BUG-015 “10 failing” is historical context from plans; this session’s measured baseline was 94 passing.
